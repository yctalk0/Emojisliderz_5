// src/components/game/ad-banner.tsx
const AdBanner = ({ position, className }: { position: string, className?: string }) => {
  return <div className={`ad-placeholder ${className}`} />;
};
export default AdBanner;
